## Dev toolchain for target repos (compatible with Python 3.8–3.13+)
pytest>=8.3
pytest-xdist>=3.5

## Coverage plugin (discriminator uses --cov/--cov-report/--cov-fail-under)
pytest-cov>=5.0

## Style/type tooling used by the discriminator ladder
black>=24.4
isort>=5.13
ruff>=0.4
flake8>=7.0
mypy>=1.10
