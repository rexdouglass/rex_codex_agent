[pytest]
addopts = -q
markers =
    unit: Fast unit tests without external dependencies.
    slow: Slow tests that are skipped by default.
norecursedirs = .git .venv build dist node_modules
testpaths =
    tests
filterwarnings =
    error
    ignore::DeprecationWarning
